# =============================================================================
# Student 5 microservices — build and validate
# =============================================================================
# Feature: Leaderboard and Analytics (Roan)
#
# Builds and tests the student 5 frontend, backend/API and database
# microservices (specification section 7.3).
#
# This workflow deliberately has NO path filter. Required status checks are
# matched by job name, and a workflow that never runs never reports a result,
# which would leave pull requests waiting forever.
# =============================================================================

name: student-5

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: student-5-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

jobs:
  # The job name below is what you add to the branch ruleset as a required check.
  student-5-build-and-test:
    name: student-5-build-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('Directory.Packages.props', '**/*.csproj') }}
          restore-keys: nuget-${{ runner.os }}-

      - name: Restore
        run: |
          dotnet restore student-5/database/LanguageWise.Student5.Db/LanguageWise.Student5.Db.csproj
          dotnet restore student-5/backend/LanguageWise.Student5.Api/LanguageWise.Student5.Api.csproj
          dotnet restore student-5/tests/LanguageWise.Student5.Api.Tests/LanguageWise.Student5.Api.Tests.csproj

      - name: Build
        run: |
          dotnet build student-5/database/LanguageWise.Student5.Db/LanguageWise.Student5.Db.csproj -c Release --no-restore
          dotnet build student-5/backend/LanguageWise.Student5.Api/LanguageWise.Student5.Api.csproj -c Release --no-restore
          dotnet build student-5/tests/LanguageWise.Student5.Api.Tests/LanguageWise.Student5.Api.Tests.csproj -c Release --no-restore

      - name: Run NUnit tests
        run: >
          dotnet test student-5/tests/LanguageWise.Student5.Api.Tests/LanguageWise.Student5.Api.Tests.csproj
          -c Release --no-build
          --logger "trx;LogFileName=student-5.trx"
          --results-directory TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: student-5-test-results
          path: TestResults/
          if-no-files-found: warn

      - name: Build the database image
        run: docker build -f student-5/database/Dockerfile -t languagewise/student-5-db:ci .

      - name: Build the backend image
        run: docker build -f student-5/backend/Dockerfile -t languagewise/student-5-backend:ci .

      - name: Build the frontend image
        run: docker build -f student-5/frontend/Dockerfile -t languagewise/student-5-frontend:ci .

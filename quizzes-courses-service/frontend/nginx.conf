# nginx configuration for the quizzes-courses-service frontend container.
#
# Serves the static build output and reverse-proxies every /api/ call to this
# service's own backend over the Docker network, so the browser talks to a
# single origin and no CORS configuration is needed.

server {
    listen      80;
    server_name _;

    root  /usr/share/nginx/html;
    index index.html;

    # Docker's embedded DNS. Resolving the upstream per request through a
    # variable means nginx still starts when the backend is not up yet.
    resolver 127.0.0.11 valid=10s ipv6=off;

    location = /health {
        access_log   off;
        default_type application/json;
        return 200 '{"status":"healthy","service":"quizzes-courses-service-frontend"}';
    }

    location = /css/theme.css {
        set $shared_frontend "http://shared-frontend:80";
        proxy_pass $shared_frontend/css/theme.css;
    }

    location /api/ {
        set $lw_backend "http://quizzes-courses-service-backend:8080";
        proxy_pass $lw_backend$request_uri;

        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_connect_timeout 5s;
        proxy_send_timeout    30s;
        proxy_read_timeout    30s;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}